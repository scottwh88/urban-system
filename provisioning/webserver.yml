---
- hosts: all

  tasks:
  - name: Install Apache Web Server
    package:
      name: httpd
      state: present

  - name: Ensure Apache is enabled and running
    service:
      name: httpd 
      state: started 
      enabled: yes
